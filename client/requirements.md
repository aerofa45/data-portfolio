## Packages
recharts | For the data visualization dashboards (line charts, bar charts, scatter plots)
framer-motion | For complex page transitions, scroll animations, and "magnetic" button effects
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
